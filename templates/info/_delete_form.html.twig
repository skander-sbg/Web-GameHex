<form method="post" action="{{ path('app_info_delete', {'contentid': info.contentid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info.contentid) }}">
    <button class="btn">Delete</button>
</form>
