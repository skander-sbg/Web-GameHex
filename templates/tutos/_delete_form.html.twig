<form method="post" action="{{ path('app_tutos_delete', {'tutoid': tuto.tutoid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tuto.tutoid) }}">
    <button class="btn">Delete</button>
</form>
